class Solution:

	def rowWithMax1s(self,arr, n, m):
		# code here
		
		row = -1
		i = 0
		j = m-1
		while i<n and j>=0:
		    if arr[i][j] == 1:
		        j -=1 
		        row = i
		    else:
		        i += 1
		return row
		        
